# Contributing

Keep adapters small and versioned. Add synthetic fixtures for every new schema and failure mode. Never commit real Codex state or conversation text. Run format, Clippy, tests, and release build before submitting changes.
